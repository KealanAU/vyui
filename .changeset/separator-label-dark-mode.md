---
"@vyui/kit": patch
---

Separator label now uses `text-highlighted` on the `<text>` itself, so its color flips with the theme (Lynx doesn't inherit the container's color into the text — the label rendered black in dark mode).

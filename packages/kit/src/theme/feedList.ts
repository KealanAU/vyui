/**
 * FeedList theme — vyui original (no nuxt/ui equivalent).
 *
 * The core `FeedList` is a virtualized native `<list>` renderer; this wrapper
 * just forwards a base class. Layout/visuals are item-driven, so the slot set
 * is intentionally minimal.
 */
export default {
  base: 'w-full h-full',
}

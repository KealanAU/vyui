{
  "name": "sortable",
  "type": "registry:ui",
  "dependencies": [
    "@vyui/core@^0.2.10"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "components/Sortable.vue",
      "target": "Sortable.vue",
      "type": "registry:ui",
      "content": "<script lang=\"ts\">\nimport theme from '@@vyui:theme/sortable'\nimport type { ThemeTV, VariantProps } from '@@vyui:composables/useStyledComponent'\n\ntype SortableTV = ThemeTV<typeof theme>\ntype SortableVariants = VariantProps<SortableTV>\n\nexport interface SortableProps<T = unknown> {\n  /** Controlled ordered items. Bind with `v-model`. */\n  modelValue?: T[]\n  /** Disable dragging on every row. */\n  disabled?: boolean\n  /**\n   * How long (ms) a press must be held before a row lifts for dragging. The\n   * hold guards against a tap or a scroll being mistaken for a drag. Set `0`\n   * for immediate drag (e.g. when the list is not inside a scroller).\n   * @defaultValue `150`\n   */\n  longPressMs?: number\n  size?: SortableVariants['size']\n  class?: any\n  ui?: Partial<Record<keyof SortableTV['slots'], any>>\n}\n\nexport interface SortableEmits<T = unknown> {\n  (e: 'update:modelValue', value: T[]): void\n}\n\nexport interface SortableSlots<T = unknown> {\n  item?(props: { item: T, index: number }): any\n}\n</script>\n\n<script setup lang=\"ts\" generic=\"T = unknown\">\nimport { computed } from 'vue'\nimport { SortableRoot, SortableItem } from '@vyui/core'\nimport { useStyledComponent } from '@@vyui:composables/useStyledComponent'\n\nconst props = withDefaults(defineProps<SortableProps<T>>(), {\n  disabled: false,\n})\nconst emit = defineEmits<SortableEmits<T>>()\ndefineSlots<SortableSlots<T>>()\n\nconst { ui } = useStyledComponent('sortable', theme, () => ({\n  size: props.size,\n  disabled: props.disabled,\n}))\n\n// Pixel item heights mirror the size variant padding so the MT swap math in\n// SortableItem lines up with the rendered row geometry.\nconst ITEM_HEIGHTS = { sm: 40, md: 52, lg: 68 } as const\n\nconst itemHeight = computed(() => ITEM_HEIGHTS[(props.size ?? 'md') as keyof typeof ITEM_HEIGHTS])\n</script>\n\n<template>\n  <SortableRoot\n    :model-value=\"modelValue\"\n    :item-height=\"itemHeight\"\n    :disabled=\"disabled\"\n    :long-press-ms=\"longPressMs\"\n    :class=\"ui.root({ class: [props.class, props.ui?.root] })\"\n    @update:model-value=\"(v: T[]) => emit('update:modelValue', v)\"\n  >\n    <SortableItem\n      v-for=\"(it, index) in modelValue\"\n      :key=\"index\"\n      :index=\"index\"\n      :class=\"ui.item({ class: props.ui?.item })\"\n    >\n      <!-- The pill is a child of the transformed shell so the shell can stay\n           transparent — paint on the shell itself travels with the drag. -->\n      <view :class=\"ui.itemContent({ class: props.ui?.itemContent })\">\n        <slot name=\"item\" :item=\"it\" :index=\"index\" />\n      </view>\n    </SortableItem>\n  </SortableRoot>\n</template>\n"
    },
    {
      "path": "theme/sortable.ts",
      "target": "theme/sortable.ts",
      "type": "registry:theme",
      "content": "/**\n * Sortable theme — light-mode Vue-Lynx defaults for a drag-to-reorder list.\n *\n * Two-layer row: `item` is the transparent shell core transforms during a drag\n * (any paint on it would travel with the finger — that's how the old divider\n * look smeared); `itemContent` is the visible pill, so only the pill appears\n * to move. The shell's bottom padding is the row gap and lives INSIDE the\n * fixed row pitch (`itemHeight` on `SortableRoot`), keeping the main-thread\n * swap math aligned with the rendered geometry.\n *\n * Size variant drives the gap, pill padding, and the optional drag handle\n * size. The `itemHeight` prop is set in pixels by the wrapper from this same\n * size token.\n */\nexport default {\n  slots: {\n    root: 'w-full min-w-0 max-w-full overflow-hidden flex flex-col',\n    item: 'group flex flex-col min-w-0 max-w-full',\n    itemContent: 'flex-1 flex flex-row items-center min-w-0 overflow-hidden gap-2 bg-default border border-default rounded-md group-ui-dragging:border-accented',\n    handle: 'shrink-0 text-dimmed',\n  },\n  variants: {\n    size: {\n      sm: {\n        item: 'pb-1.5',\n        itemContent: 'px-4 text-base',\n        handle: 'size-5',\n      },\n      md: {\n        item: 'pb-2',\n        itemContent: 'px-5 text-lg',\n        handle: 'size-6',\n      },\n      lg: {\n        item: 'pb-2',\n        itemContent: 'px-6 text-xl',\n        handle: 'size-7',\n      },\n    },\n    disabled: {\n      true: { item: 'opacity-50 cursor-not-allowed' },\n    },\n  },\n  defaultVariants: {\n    size: 'md' as const,\n  },\n}\n"
    }
  ]
}

---
title: useEmitAsProps
description: Convert a component's emit function into onXxx handler props, since Vue has no built-in emit forwarding.
navigation:
  icon: i-lucide-zap
links:
  - label: Source
    icon: i-simple-icons-github
    to: https://github.com/KealanAU/vyui/blob/main/packages/core/src/shared/useEmitAsProps.ts
    target: _blank
---

::callout{icon="i-lucide-flask-conical"}
Advanced. This is a primitive-authoring utility from `@vyui/core` that most app code never calls directly.
::

## Overview

```ts
import { useEmitAsProps } from '@vyui/core'

const emitsAsProps = useEmitAsProps(emit)
```

Converts a component's `emit` function into an object of `onXxx` handler props. Vue has no built-in "forward my emits to a child" mechanism ([vuejs/core#5917](https://github.com/vuejs/core/issues/5917)), so this lets a wrapper spread both props and events onto an inner element transparently.

## Signature

`useEmitAsProps<Name extends string>(emit: (name: Name, ...args: any[]) => void): Record<string, (...args: any[]) => void>`

## Related

- [useForwardPropsEmits](/composables/use-forward-props-emits)
- [useForwardProps](/composables/use-forward-props)
